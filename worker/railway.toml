# Railway configuration for the Worker service
# Forces use of Dockerfile builder for Playwright browser installation

[build]
# DOCKERFILE (uppercase) is required - tells Railway to use Docker instead of Railpack
builder = "DOCKERFILE"
# Path relative to the service's root directory (worker/)
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/index.js"
